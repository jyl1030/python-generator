A simple flask app translating natural language instructions (ie. convert multiple integers into a single integer.) into a python script (ie. combined_str = ''.join(str(i) for i in ints), using an open source LLM.
